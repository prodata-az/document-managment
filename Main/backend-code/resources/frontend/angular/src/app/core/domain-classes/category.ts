export interface Category {
  id?: string;
  name: string;
  square: string;
  farmerid: string;
  violation: string;
  status: string;
  district: string;
  description: string;
  parentId?: string;
  deafLevel?: number;
  index?: number;
}
